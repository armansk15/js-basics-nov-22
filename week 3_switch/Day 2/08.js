function onTimeForExam(input) {
    let examHour = Number(input.shift());
    let examMinute = Number(input.shift());
    let arrivingHour = Number(input.shift());
    let arrivingMinute = Number(input.shift());
    
    let examTime = (examHour * 60) + examMinute;
    let arrivingTime = (arrivingHour * 60) + arrivingMinute;
    let timeDifference;

    

    
}

